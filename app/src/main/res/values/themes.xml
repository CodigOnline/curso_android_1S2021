<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- SplashScreen -->
    <style name="Splash" parent="Base.MyTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/transparent</item>
        <!-- Booleano false si el fondo es oscuro o true si el fondo es claro-->


        <!-- DIVIDER-->
        <item name="android:navigationBarDividerColor">@color/color_secondary</item>
        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/transparent</item>


          <!--<item name="android:windowLightStatusBar">true</item>-->
        <item name="android:windowLightNavigationBar">true</item>-->
    </style>

    <!-- Base application theme. -->
    <style name="Base.MyTheme" parent="Base.MaterialThemeBuilder">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/color_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/color_on_secondary</item>
        <item name="android:colorBackground">@color/color_background</item>
        <item name="colorOnBackground">@color/color_on_background</item>
        <item name="colorSurface">@color/color_surface</item>
        <item name="colorOnSurface">@color/color_on_surface</item>
        <item name="colorError">@color/color_error</item>
        <item name="colorOnError">@color/color_on_error</item>


        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Transitions -->
        <item name="android:windowEnterTransition">@transition/fade_in</item>
        <item name="android:windowExitTransition">@transition/slide_end</item>
    </style>

    <style name="Base.MaterialThemeBuilder" parent="Theme.MaterialComponents.DayNight.NoActionBar"></style>
</resources>